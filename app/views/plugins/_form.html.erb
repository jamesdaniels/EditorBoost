<%= form_for(@plugin, :url => @plugin.new_record? && plugins_path(:editor => @editor.short) || plugin_path(@plugin, :editor => @editor.short)) do |f| %>
  <% if @plugin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plugin.errors.count, "error") %> prohibited this plugin from being saved:</h2>

      <ul>
      <% @plugin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :readme %><br />
    <%= f.text_area :readme %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
